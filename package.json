{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node app",
    "build": "npm ci --prefix client && npm --prefix client run build && node scripts/sync-client-build-to-public.js",
    "vercel-build": "npm ci --prefix client && npm --prefix client run build && node scripts/sync-client-build-to-public.js",
    "prisma:generate": "cd prisma && prisma generate --schema schema.prisma",
    "prisma:push": "cd prisma && prisma db push --schema schema.prisma",
    "prisma:validate": "cd prisma && prisma validate --schema schema.prisma",
    "test": "echo \"Error: no test specified\" && exit 1",
    "push1": "git push origin toPrisma2 && git push ai-jobs toPrisma2:master && git push alb-jobs toPrisma2:master"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "express-react-views": "^0.11.0",
    "graphql-yoga": "^1.17.4",
    "html-to-text": "^5.1.1",
    "jsonwebtoken": "^8.5.1",
    "mailgun-js": "^0.22.0",
    "morgan": "^1.9.1",
    "node-cron": "^2.0.3",
    "nodemailer": "^6.2.1",
    "prop-types": "^15.7.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-lottie": "^1.2.3",
    "stripe": "^7.1.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
